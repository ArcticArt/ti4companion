<Project>

  <!-- The product version, in ONE place. Every project in the solution inherits it, so the assemblies,
       the published binaries and the version the app shows in its footer can never drift apart.
       Bump it here and nowhere else. -->
  <PropertyGroup>
    <Version>2.0.0</Version>
  </PropertyGroup>

</Project>
