@inherits Ti4ComponentBase

@* Placeholder for the upcoming production planner. The structured unit data (cost/produced/…) already
   ships in the content bundle (Store.ActiveUnits); this tab will host the build calculator. *@
<div class="panel production-wip">
    <img src="units/Warsun.png" alt="" class="wip-ship" />
    <h3>@Loc["nav.production"]</h3>
    <p class="faint">@Loc["production.wip"]</p>
    <span class="chip chip-active wip-badge">WIP</span>
</div>
