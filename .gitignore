# Build output
[Bb]in/
[Oo]bj/
artifacts/
publish/

# Visual Studio / Rider / VS Code
.vs/
.idea/
*.user
*.suo
*.userosscache
*.sln.docstates

# .NET
project.lock.json
*.nupkg

# SQLite session DB (created at runtime; back it up separately)
ti4.db
ti4.db-shm
ti4.db-wal
# ti4master.db (the master content DB) is COMMITTED as the content artifact — its transient
# WAL/shared-memory sidecars are not.
ti4master.db-shm
ti4master.db-wal

# Environment / secrets
.env
.env.local
*.env.local

# Claude local settings + throwaway preview config
.claude/settings.local.json
.claude/launch.json

# OS / editor cruft
Thumbs.db
.DS_Store
*.swp

# Logs / temp
*.log
_secrets.txt
_tmp*

# Local-only source assets — kept OUT of the repo (large binaries + licensed/copyrighted material):
# GIMP card-art templates (~69 MB), commercial fonts (Myriad Pro/Adobe, Handel Gothic),
# and faction source art (likely FFG-copyrighted). These live on the dev machine only.
/xcf/
/Resources/
/References/Fonts/
